# Database (Supabase Postgres or local Postgres)
# Example Supabase Transaction Pooler:
# postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
DATABASE_URL=postgresql://poncheo:poncheo_dev@localhost:5432/poncheo

# Authentication
JWT_SECRET=change-this-in-production-please
JWT_EXPIRES_IN=24h

# Server
PORT=3000
NODE_ENV=development

# Frontend (for CORS)
CORS_ORIGIN=http://localhost:5173

# Auto-close orphan punches
AUTO_CLOSE_CRON=0 2 * * *
AUTO_CLOSE_THRESHOLD_HOURS=14

# Secret for /api/jobs/auto-close (Vercel Cron / external scheduler)
CRON_SECRET=change-this-cron-secret

# Frontend only (set in Vercel frontend project env vars)
# VITE_API_BASE_URL=https://<your-backend-domain>/api
